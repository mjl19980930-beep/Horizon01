name: AI Topic Collector to Feishu

on:
  workflow_dispatch:
    inputs:
      hours:
        description: "抓取最近多少小时"
        required: false
        default: "72"
      limit:
        description: "最多写入多少条"
        required: false
        default: "20"
  schedule:
    - cron: "30 1 * * *"
    - cron: "30 7 * * *"

permissions:
  contents: write

jobs:
  collect-and-write:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Collect AI topics and write to Feishu
        env:
          OPENAI_API_KEY: ${{ secrets.DEEPSEEK_API_KEY || secrets.Deepseek_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN_FOR_HORIZON || github.token }}
          FEISHU_APP_ID: ${{ secrets.FEISHU_APP_ID }}
          FEISHU_APP_SECRET: ${{ secrets.FEISHU_APP_SECRET }}
          FEISHU_WIKI_TOKEN: ${{ secrets.FEISHU_WIKI_TOKEN }}
          FEISHU_BITABLE_APP_TOKEN: ${{ secrets.FEISHU_BITABLE_APP_TOKEN }}
          FEISHU_TABLE_ID: ${{ secrets.FEISHU_TABLE_ID }}
          FEISHU_AUTO_CREATE_FIELDS: "true"
        run: |
          mkdir -p outputs
          set -o pipefail
          if [ -z "$OPENAI_API_KEY" ]; then
            echo "DEEPSEEK_API_KEY is missing. Please add it in GitHub Actions secrets."
            exit 1
          fi
          python scripts/run_topic_collector_weiyu.py \
            --hours "${{ github.event.inputs.hours || '72' }}" \
            --limit "${{ github.event.inputs.limit || '20' }}" 2>&1 | tee outputs/workflow.log

      - name: Save run state and preview
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .state outputs || true
          if ! git diff --cached --quiet; then
            git commit -m "chore: update AI topic collector run state"
            git pull --rebase || true
            git push
          else
            echo "No run state changes to commit"
          fi